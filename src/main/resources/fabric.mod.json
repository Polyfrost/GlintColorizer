{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"Polyfrost",
		"lowercasebtw"
	],
	"contributors": [
	],
	"contact": {
		"homepage": "${source}",
		"sources": "${source}",
		"issues": "${issues}"
	},
	"license": "${license}",
	"icon": "assets/${id}/glintcolorizer_dark.svg",
	"environment": "client",
	"depends": {
		"fabricloader": ">=${fabric_loader_version}",
		"minecraft": "${minecraft_version_range}",
		"fabric-api": "*"
	}
}
